# v0.1-shipped

- initial public ship of `gxp-change-control-board`
- C# / ASP.NET biotech operator surface for change classification, validation evidence gaps, approval continuity, and release-safe cutover posture
- live routes: `/`, `/change-board`, `/control-gaps`, `/release-posture`, `/verification`, `/docs`
- synthetic sample data only
- Pages deploy with `gxp.kineticgain.com`
